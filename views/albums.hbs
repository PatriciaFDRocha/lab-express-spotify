<div class="container">
    <div class="col-md-3">
        {{#each albums}}
            <div class="row">
                <img src={{images.1.url}} alt="image">
                <h3>{{name}}</h3>
                <button><a href="/tracks/{{id}}">View Tracks</a></button> 
            </div>
        {{/each}}
    </div>
</div>